# PLACEHOLDER_APP_NAME

A simple application built with preeact, TypeScript, Vite, and Cloudflare Workers.

You can view a deployed version at https://PLACEHOLDER_KV_NAMESPACE.lkbm.workers.dev/

# Run locally
* nvm use
* nvm install
* npm run dev

# Deploy

* npx wrangler deploy
